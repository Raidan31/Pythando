# Filtrar lista
numeros = [1, 30, 21, 2, 9, 65, 34]
#COMPRESSION
pares = [numero for numero in numeros if numero % 2 == 0]
# nova lista = [nova lista2 que irá aplicar a modificação for nova lista2 que irá aplicar a modificação in lista a ser modificada e O if nova lista2 q será executado primeio)] 
print(pares)

# Modificar valores
numeros = [1, 30, 21, 2, 9, 65, 34]
quadrado = [numero**2 for numero in numeros]
# nova lista = [nova lista2 que irá aplicar a modificação for nova lista2 que irá aplicar a modificação in a lista a ser modificada ]
print(quadrado)
